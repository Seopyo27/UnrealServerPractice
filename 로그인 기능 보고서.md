# 로그인 및 Dedicated Server 연동 기능 정리

## 1. 개요

이 프로젝트에는 테스트용 웹서버를 이용해 사용자를 로그인시키고, 로그인에 성공한 클라이언트를 Unreal Dedicated Server로 접속시키는 기능이 구현되어 있다.

전체 흐름은 다음과 같다.

1. 웹서버를 실행한다.
2. Dedicated Server가 웹서버에 자신의 주소를 등록한다.
3. 클라이언트가 로그인 화면에서 ID와 비밀번호를 입력한다.
4. 웹서버가 계정을 확인하고 게임 서버 주소를 반환한다.
5. 클라이언트가 5초 뒤 게임 서버에 접속한다.

## 2. 웹서버 파트

대상 파일: `WebServer/server.js`

Node.js 기본 HTTP 모듈을 사용하는 테스트용 웹서버가 구현되어 있다. 별도의 npm 패키지는 필요하지 않으며 기본 포트는 `8080`이다.

구현된 API는 다음과 같다.

| API | 구현 내용 |
|---|---|
| `GET /health` | 웹서버 동작 여부와 등록된 게임 서버 수를 반환한다. |
| `POST /api/servers/register` | Dedicated Server의 ID, 포트, 맵 정보를 등록한다. |
| `POST /api/login` | 입력한 ID와 비밀번호를 확인하고 게임 서버 주소를 반환한다. |

테스트 계정은 아래와 같이 코드에 고정되어 있다.

- ID: `testuser`
- 비밀번호: `test1234`

등록된 게임 서버 정보는 메모리에 저장되므로 웹서버가 종료되면 사라진다.

## 3. 게임 서버 등록 파트

대상 파일: `Source/L20260713_Day03/Network/ServerDirectorySubsystem.cpp`

Unreal Dedicated Server가 시작되면 `UServerDirectorySubsystem`이 자동으로 등록 API를 호출한다.

웹서버에 전달하는 정보는 다음과 같다.

- 서버 ID: `default-server`
- 서버 포트: `7777`
- 현재 맵 이름
- 서버 상태: `online`

웹서버는 요청을 보낸 게임 서버의 IP 주소를 확인해 서버 ID와 함께 저장한다. 등록 성공과 실패 결과는 Unreal 이벤트로 전달된다.

## 4. 로그인 화면 파트

대상 파일:

- `Source/L20260713_Day03/UI/LoginWidget.h`
- `Source/L20260713_Day03/UI/LoginWidget.cpp`
- `Content/Blueprints/WBP_Login.uasset`

로그인 화면에는 다음 UI가 연결되어 있다.

- ID 입력란: `UserIdInput`
- 비밀번호 입력란: `PasswordInput`
- 로그인 버튼: `LoginButton`
- 상태 메시지: `StatusText`

ID 또는 비밀번호가 비어 있으면 요청을 보내지 않고 입력 안내 문구를 표시한다. 로그인 요청 중에는 상태를 `로그인 중...`으로 변경하고 중복 클릭을 막기 위해 로그인 버튼을 비활성화한다.

## 5. 로그인 통신 파트

대상 파일: `Source/L20260713_Day03/Network/ServerDirectorySubsystem.cpp`

클라이언트는 사용자가 입력한 정보를 JSON으로 만들어 웹서버의 `/api/login`에 전송한다.

```json
{
  "user_id": "testuser",
  "password": "test1234"
}
```

로그인 성공 시 웹서버는 다음과 같이 게임 서버의 IP와 포트를 반환한다.

```json
{
  "server_ip": "127.0.0.1",
  "server_port": 7777
}
```

클라이언트는 받은 IP와 포트를 `127.0.0.1:7777` 형태의 접속 주소로 조합한다. 잘못된 계정, 서버 미등록, 네트워크 실패, 잘못된 JSON 응답도 실패로 처리하도록 구현되어 있다.

## 6. 로그인 성공 및 서버 접속 파트

대상 파일:

- `Source/L20260713_Day03/UI/LoginWidget.cpp`
- `Source/L20260713_Day03/Network/ServerDirectorySubsystem.cpp`

로그인에 성공하면 다음 동작을 수행한다.

1. 클라이언트 내부 인증 상태를 `true`로 저장한다.
2. `로그인 성공. 5초 후 게임 서버에 접속합니다.` 메시지를 표시한다.
3. 5초 동안 대기한다.
4. 로그인 화면을 제거한다.
5. 마우스와 입력 모드를 게임 플레이용으로 변경한다.
6. Unreal의 `ClientTravel`을 호출해 Dedicated Server로 이동한다.

인증 상태는 `GameInstanceSubsystem`에 저장되므로 맵 이동 후에는 로그인 화면이 다시 표시되지 않는다.

## 7. 로그인 화면 생성 파트

대상 파일: `Source/L20260713_Day03/Base/MyPC.cpp`

플레이어 컨트롤러가 시작될 때 아래 조건을 모두 만족하면 로그인 위젯을 생성한다.

- Dedicated Server가 아닌 클라이언트이다.
- 로컬 플레이어 컨트롤러이다.
- 아직 로그인 성공 상태가 아니다.
- 로그인 위젯 클래스가 설정되어 있다.

로그인 화면이 표시되는 동안에는 UI 전용 입력 모드가 적용되고 마우스 커서가 표시된다.

## 8. 설정 파트

대상 파일: `Config/DefaultGame.ini`

웹서버 연결에 필요한 값이 설정 파일에 분리되어 있다.
